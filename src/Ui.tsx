import { createGlobalStyle } from 'styled-components';

export const UiStyle = createGlobalStyle`
    .isFixedScroll {
        overflow: hidden;
    }

    #root {
        overflow: hidden;
    }

    .root_section {
        padding-top: 65px;
    }
`;
